---
const rays = Array.from({ length: 24 }, (_, i) => i * 15)
---

<svg
  class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-[55%] w-[1000px] h-[1000px] opacity-[0.04] animate-[sunburst-spin_120s_linear_infinite] z-0"
  viewBox="0 0 800 800"
>
  <g transform="translate(400,400)">
    {rays.map((deg) => (
      <polygon
        points="0,-400 25,-140 -25,-140"
        fill="var(--color-burnt-orange)"
        transform={`rotate(${deg})`}
      />
    ))}
  </g>
</svg>
